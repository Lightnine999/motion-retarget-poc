# 모션 리타겟팅 POC — PRD

> STATUS: DRAFT
> 작성일: 2026-09-22

## 1. 배경 & 목적

유튜브 영상, 직접 촬영 영상, AI 생성 영상 등 "사람이 나오는 영상" 하나에서 그 사람의 동작(모션)을 뽑아내서, 전혀 다른 3D 캐릭터에게 똑같은 동작을 시킬 수 있는지 **기술적으로 검증**한다.

이 문서는 완성된 제품의 요구사항이 아니라, **POC(개념 검증)를 위한 최소 요구사항**을 정리한 것이다. "얼마나 예쁘게 만드느냐"보다 "핵심 기술 두 가지가 실제로 동작하느냐"를 확인하는 데 목적이 있다.

## 2. 대상 사용자

- 현재 학습 중인 팀원들. 제작 과정과 결과물을 공유해서 함께 보고 피드백을 주고받는다.
- 외부 배포·상업적 사용은 고려하지 않는다.

## 3. 완성도 기준

- 디테일(모션 정확도, 렌더 퀄리티 등)이 다소 떨어져도 괜찮다.
- 대신 아래 **핵심 기술 두 가지**는 반드시 동작하는 것을 눈으로 확인할 수 있어야 한다.
- 빠른 시간 내에 "되는지 안 되는지"를 판단하는 것이 최우선 목표다.

## 4. 핵심 기능 (Core Features)

### 4.1 핵심 기술 1 — 오픈소스 3D 모션 리타겟팅 (메인)

입력 영상 속 인물의 동작을 3D 모션 데이터로 추정하고, 그 모션을 완전히 다른 3D 리깅 캐릭터(Mixamo 캐릭터)에 그대로 입혀서, **그 캐릭터가 원본 인물과 같은 동작을 하는 영상**을 만들어낸다.

- 입력: 사람 1명이 나오는 영상 클립 1개 (5~15초 권장)
- 출력: Mixamo 리깅 캐릭터가 같은 동작을 수행하는 렌더링된 MP4 영상
- 확인 방법: 팀원이 Blender를 설치하거나 열어보지 않고도, MP4 파일 하나로 결과를 볼 수 있어야 한다.

### 4.2 핵심 기술 2 — 상용 AI 영상 서비스 키프레임→모션 비교 (참고자료)

Kling, 힉스필드, Seedance 2.0 같은 상용 AI 영상 생성 서비스가 제공하는 "키프레임 이미지 + 모션 참조 영상 → 그 이미지 속 인물/캐릭터가 그 동작을 하는 사실적 영상" 기능을 API로 돌려보고, 결과가 실제로 매끄러운지 비교한다.

- 입력: 동일한 키프레임 이미지 + 동일한 모션 참조 영상(4.1과 같은 원본 영상 활용 가능)
- 출력: 서비스별 결과 영상 + 비교 정리표(매끄러움, 정확도, 비용, 소요 시간)
- 성격: 자체 구현이 아니라 **외부 서비스 품질 검증**이 목적이므로 스크립트는 API 호출·결과 저장 위주로 가볍게 유지한다.

### 4.3 두 핵심 기술의 관계

핵심 기술 1과 2는 **서로 다른 출력 형태**를 가진 독립적인 실험이다 (1은 3D 리깅 캐릭터, 2는 2D 사실적 영상). 하나의 파이프라인으로 합치지 않고, 같은 입력 영상을 각각 통과시켜 두 갈래 결과를 나란히 놓고 비교하는 방식으로 진행한다.

## 5. 범위 (Scope)

### 포함 (In Scope)
- 영상 1개를 입력받아 4.1의 3D 리타겟팅 파이프라인을 통과시키는 스크립트
- 같은 입력(또는 파생 키프레임)을 4.2의 상용 서비스 API 3종에 보내고 결과를 저장하는 스크립트
- 두 결과를 정리해서 팀원에게 공유할 수 있는 형태(README + 결과 영상/표)
- 유튜브 영상은 로컬 파일로 다운로드해서 동일 파이프라인에 태우는 것까지 포함 (다운로드 자체는 기존 오픈소스 도구 활용)

### 제외 (Out of Scope)
- 웹/앱 UI, 사용자 업로드 인터페이스 등 "제품"으로서의 기능
- 여러 사람이 동시에 나오는 영상, 복잡한 카메라 워크(줌/컷 전환) 대응
- 손가락 관절, 표정 등 정밀 디테일 리타겟팅
- 실시간 처리, 상업적 배포, 대량 처리 파이프라인

## 6. 성공 기준 (Definition of Done)

1. 유튜브(또는 촬영) 영상 1개를 넣었을 때, Mixamo 캐릭터가 그 동작을 흉내 내는 MP4 영상이 나온다.
2. Kling / 힉스필드 / Seedance 2.0 중 최소 2개 이상의 서비스로 같은 입력에 대한 결과 영상을 뽑아 비교표를 만든다.
3. 팀원이 코드를 실행하지 않고도 결과물(영상 파일들)만으로 "이 기술이 되는지 안 되는지"를 판단할 수 있다.
4. 각 단계에서 막힌 지점과 한계를 README에 솔직하게 기록한다 (완벽하게 동작하지 않아도 무엇이 왜 안 됐는지가 남아야 한다).

## 7. 제약 & 전제

- 실행 환경은 로컬 Mac(Apple Silicon, MPS)과 Google Colab GPU를 혼합해서 쓴다. 무거운 딥러닝 추론(포즈 추정)은 Colab, 나머지(영상 준비, Blender 리타겟/렌더링, API 호출)는 로컬에서 처리한다.
- 유튜브에서 가져오는 영상은 저작권이 있는 콘텐츠이므로, 팀 내부 학습·실험 목적으로만 사용하고 재배포하지 않는다.
- 상용 AI 서비스는 유료이므로, 실제 API 호출 전에 예상 비용을 확인하고 진행한다.
- 이 저장소(`motion-retarget-poc`)는 아이펠 실습 워크스페이스(`aiffel_work`)와 독립된 별도 저장소다.

## 8. 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| GVHMR 셋업(SMPL 라이선스 동의, 체크포인트 다운로드)이 예상보다 오래 걸림 | 핵심 기술 1 착수 지연 | 초기에 셋업만 먼저 검증하는 스파이크 진행 |
| Mixamo↔Blender 리타겟팅이 캐릭터 스켈레톤 차이로 어색하게 나옴 | 결과물 완성도 저하 | POC 완성도 기준상 허용, 한계로 기록 |
| 상용 서비스 API 비용/쿼터 제한 | 비교 실험 축소 | 서비스당 최소 1~2회 호출로 제한, 사전 승인 |
| 유튜브 영상 저작권 이슈 | 법적/윤리적 리스크 | 내부 학습용, 재배포 금지 명시 |

## 9. 열린 질문 (Open Questions)

- Colab 세션 시간 제한 내에 GVHMR 추론이 안정적으로 끝나는지는 실제로 돌려봐야 안다.
- Higgsfield는 공개 API가 없고 Python SDK(`higgsfield-client`)/CLI 기반이라, 다른 두 서비스와 연동 난이도가 다를 수 있다.
